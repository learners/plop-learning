<template>
  <div class="{{ dashCase name }}">
    {{ dashAround 123 }}
    {{ dashAround (properCase "aa-bb") }}

    {{> salutation greeting="Hello there" }} greeting为默认参数

    {{#if toppings}}
      执行if {{ wordJoin toppings }}
    {{else}}
      执行else
    {{/if}}

    package.json 的 name 字段：{{ pkg 'name' }}
  </div>
</template>

<script>
export default {
  name: '{{ properCase name }}'
}
</script>
